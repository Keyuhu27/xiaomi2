# [部署到 Railway 时新增] 生产构建（vite build --mode production）专用配置。
# 留空表示走"跟页面同源"的相对路径，由 vite.config.ts 里的 preview.proxy
# 转发到容器内部的后端，不需要给后端单独暴露一个公网域名。
# 本地开发（vite dev）不受这个文件影响，还是用 .env 里的 http://127.0.0.1:8080。
SERVICE_BASE_URL=""
